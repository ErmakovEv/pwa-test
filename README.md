### PWA Push Demo

## 📦 Описание

**PWA Push Demo** — простое PWA‑приложение на React, которое демонстрирует работу web push‑уведомлений без внешних сторонних сервисов (только стандартные Web Push API браузера).

Основной флоу:

- приложение просит **разрешить уведомления** и предлагает **установить себя на главный экран** (PWA);
- на странице можно **запланировать push** на нужное время, задать **заголовок, текст и паттерн вибрации**;
- в назначенное время приходит push‑уведомление **даже если вкладка/приложение закрыты**;
- если один и тот же пользователь открыл приложение на нескольких устройствах, **push придёт на все эти устройства**.

---

## ✅ Чек‑лист реализации

- [x] **PWA‑приложение на React**
  - [x] `manifest.webmanifest`, `service worker`, установка на home screen
- [x] **Запрос разрешения на уведомления**
  - [x] Кнопка «Разрешить и подписаться»
  - [x] Отображение текущего статуса `Notification.permission`
- [x] **Планирование push‑уведомлений на заданное время**
  - [x] Поле `datetime-local` для выбора времени в будущем
  - [x] Параметры уведомления: заголовок, текст, паттерн вибрации
- [x] **Отправка push‑уведомлений с бэкенда**
  - [x] Node/Express + `web-push`
  - [x] Работа без внешних сервисов (используем собственные VAPID‑ключи)
- [x] **Получение пушей при закрытом приложении**
  - [x] Обработка события `push` в `sw.js` и показ системного уведомления
  - [x] `notificationclick` открывает/фокусирует PWA
- [x] **Поддержка нескольких устройств**
  - [x] Один `userId` → несколько подписок (разные endpoints)
  - [x] Рассылка пуша по всем подпискам пользователя
- [x] **API‑слой**
  - [x] `POST /api/subscribe` — сохранение подписки
  - [x] `POST /api/schedule` — планирование отправки
  - [x] `GET /api/vapidPublicKey` — отдача публичного ключа фронту
  - [x] JSON‑ответ для неизвестных `/api/*` (`404 { error: 'API route not found!' }`)
- [x] **PWA‑обвязка и фронтенд‑UI**
  - [x] Простая, но аккуратная форма без лишних файлов и стилей
  - [x] Обработка ошибок с подсказками через `alert` и консоль

---

## ⚙️ Требования

- **Node.js**: v18+
- **npm**: v8+
- Современный браузер с поддержкой:
  - Service Worker
  - Web Push API
  - PWA (Chrome, Edge, Firefox, мобильные Chrome/Edge/Brave и т.п.)

---

## 🔑 Настройка `.env`

В корне репозитория должен быть файл `.env`:

```env
VAPID_PUBLIC_KEY=ВАШ_ПУБЛИЧНЫЙ_VAPID_КЛЮЧ
VAPID_PRIVATE_KEY=ВАШ_ПРИВАТНЫЙ_VAPID_КЛЮЧ
VAPID_EMAIL=mailto:you@example.com
PORT=3000
```

### Как сгенерировать VAPID‑ключи

В корне проекта:

```bash
cd /Users/evgenijermakov/projects/tests/pwa-test
npx web-push generate-vapid-keys
```

В выводе будут:

```text
Public Key:
BExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

Private Key:
Xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

Подставь их в `.env` **без кавычек, без пробелов и без `=` в конце**:

```env
VAPID_PUBLIC_KEY=BExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
VAPID_PRIVATE_KEY=Xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
VAPID_EMAIL=mailto:you@example.com
PORT=3000
```

---

## 🚀 Запуск проекта (dev‑режим)

В корне проекта:

```bash
cd /Users/evgenijermakov/projects/tests/pwa-test

# Установка зависимостей (корень + фронт)
npm install
cd frontend && npm install
cd ..

# Запуск dev-сборки фронта и сервера одновременно
npm run dev
```

Что произойдёт:

- `dev:server` запустит `backend/server.js` (API и статику на `http://localhost:3000`)
- `dev:client` запустит Vite (например, `http://localhost:5174/` — смотри в терминал)

Открой в браузере:

```text
http://localhost:5174/
```

---

## 🏭 Сборка и запуск в прод‑режиме

```bash
cd /Users/evgenijermakov/projects/tests/pwa-test

# Собрать фронтенд
cd frontend
npm run build
cd ..

# Запустить только Node-сервер
npm start
```

Теперь:

- Сервер слушает `http://localhost:3000`
- Отдаёт:
  - статику из `frontend/dist`
  - SPA‑fallback (`index.html`) для всех не‑API путей
  - API‑эндпоинты `/api/*`

Открой в браузере:

```text
http://localhost:3000/
```

---

## 🔔 Разрешения на уведомления и системные настройки

Чтобы пуши реально показывались, нужно:

- **В браузере:**
  - При первом нажатии на кнопку **«Разрешить и подписаться»** появится диалог браузера «Разрешить уведомления».
  - Выбери **«Разрешить»**.
  - Если случайно запретил — в Chrome:
    - нажми на значок «замка» слева от URL;
    - найди «Уведомления» → поставь «Разрешить»;
    - перезагрузи страницу.

- **В ОС:**
  - Убедись, что **уведомления для браузера включены в системных настройках**:
    - **macOS**: `Системные настройки → Уведомления → Google Chrome / ваш браузер` → включить уведомления.
    - **Windows**: `Параметры → Система → Уведомления` → включить для браузера.
    - **Android**: `Настройки → Приложения → Chrome → Уведомления` → включить.
  - Если на уровне ОС уведомления отключены, браузер может «разрешить», но пуши всё равно не будут появляться.

---

## 📱 Установка PWA на главный экран

### Chrome / Edge (desktop)

1. Открой страницу (`http://localhost:5174/` или `http://localhost:3000/` в проде).
2. Если браузер считает, что приложение можно установить, сработает `beforeinstallprompt`, и в UI:
   - кнопка **«Установить приложение»** станет активной;
   - при клике появится нативный диалог установки.
3. Можно также установить через меню браузера (например, **«Установить приложение…»** в Chrome).

### Android (Chrome)

1. Открой приложение в Chrome.
2. Через пару секунд появится баннер «Добавить на главный экран» / «Установить приложение».
3. Либо в меню **⋮** → **«Установить приложение»**.

После установки:

- приложение открывается как отдельное PWA‑окно;
- push‑уведомления приходят и при закрытой вкладке.

---

## 🔄 Флоу использования

1. Открыть страницу PWA.
2. Нажать **«Разрешить и подписаться»**:
   - браузер спросит про разрешение на уведомления;
   - при `granted` произойдёт подписка на push и отправка подписки на бэкенд.
3. При желании установить PWA на home screen (кнопка установки или меню браузера).
4. Заполнить форму:
   - время (будущее) через `datetime-local`;
   - заголовок, текст;
   - паттерн вибрации (например, `200,100,200`).
5. Нажать **«Запланировать»** — бэкенд поставит `setTimeout` и в нужный момент отправит push на **все подписанные устройства** этого `userId`.


